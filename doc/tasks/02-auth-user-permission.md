# 02 用户、认证与权限

## 目标
完成 UID 登录、用户权限维护、会话用户查询和系统设置基础能力。

## 参考文档
- `企业办公_点检AI方案设计.md`：4.1 用户与权限管理。
- `CheckFlow 后端 API 设计 v0 1.md .md`：Auth API、User & Permission API、System Setting API。
- `CheckFlow 数据库设计 v1 0.md`：用户、权限与系统设置表。

## 前置依赖
- [x] 00 项目初始化完成。
- [x] 01 数据库迁移与 ORM 完成。

## 当前状态复核

当前状态为 🟡 最小链路可用、完整验收未完成。总状态以 `doc/tasks/progress.md` 为准，本文件用于记录本模块的具体完成项和待补缺口。

已具备最小实现：

- UID 登录、`GET /api/v1/auth/me`、用户列表、创建用户、编辑用户、系统设置读取与保存已接通。
- 后端已有用户权限更新、启用、停用和删除访问配置接口；删除访问配置为软删除，历史业务数据保留。
- `super_admin` 仅作为“权限管理”的内部兼容编码，不再自动获得点检、规则或项目管理能力。
- 系统已保护至少保留一个启用且具备权限管理的账号，并阻断停用、删除当前登录账号和取消自己的权限管理权限。
- 新增用户、权限变更、启用/停用、删除访问配置和系统设置保存已有基础审计日志。

仍未完整：

- 当前后台页已支持账号搜索、权限/状态筛选、多权限编辑、启用/停用、删除访问配置和只读模式提示。
- 全局各业务页的权限态导航、只读/不可操作态仍需在对应模块继续补齐。

## 最小任务清单
- [x] T01：实现 `POST /api/v1/auth/login`；验收：有效 UID 可登录，停用 UID 被阻断。
- [x] T02：实现 `GET /api/v1/auth/me`；验收：返回当前用户与权限集合。
- [x] T03：实现用户列表、创建用户接口；验收：可按 UID、姓名、邮箱、权限和状态查询。
- [x] T04：实现后端用户权限更新接口；验收：同一 UID 可配置多个权限。
- [x] T05：实现启用、停用、删除访问配置；验收：不能停用、删除当前登录账号，且系统保留至少一个启用的权限管理员。
- [x] T06：实现系统设置读取与保存；验收：自动检查开关可持久化并记录保存人。
- [x] T07：实现前端登录页和后台账号管理页；验收：后台页支持权限更新、启用/停用、删除访问配置、无权限只读/不可操作态。
- [x] T08：写入基础权限变更审计日志；验收：新增、停用、权限变更均可追溯。

## 验收标准
- [x] UID 登录流程可跑通。
- [x] 当前用户接口可返回权限集合。
- [x] 后端权限支持叠加，不互相收窄。
- [x] 后端至少保留一个启用的权限管理员。
- [x] 历史数据不因账号停用或删除而丢失。
- [x] 前端可维护同一用户的多权限。
- [x] 前端可启用、停用账号，并展示明确状态变化。
- [x] 前端可删除访问配置，且不能删除最后一个权限管理员。
- [x] 无权限用户进入后台时只读或不可操作，并有明确提示。
- [x] 权限维护、账号启停用和删除访问配置有专项测试覆盖。

## 注意事项
CheckFlow 不维护公司账号密码；MVP 可使用本地密码校验或模拟认证，但接口边界需保留公司账号体系接入点。
