# 02 用户、认证与权限

## 目标
完成 UID 登录、用户权限维护、会话用户查询和系统设置基础能力。

## 参考文档
- `企业办公_点检AI方案设计.md`：4.1 用户与权限管理。
- `CheckFlow 后端 API 设计 v0 1.md .md`：Auth API、User & Permission API、System Setting API。
- `CheckFlow 数据库设计 v1 0.md`：用户、权限与系统设置表。

## 前置依赖
- [x] 00 项目初始化完成。
- [x] 01 数据库迁移与 ORM 完成。

## 当前状态复核

当前状态为 🟡 后台账号管理最小链路可用、完整验收未完成。总状态以 `doc/tasks/progress.md` 为准，本文件用于记录本模块的具体完成项和待补缺口。

已具备最小实现：

- UID 登录、`GET /api/v1/auth/me`、用户列表、创建用户、编辑用户、系统设置读取与保存已接通。
- 后端已有用户权限更新、启用、停用和删除访问配置接口；删除访问配置为软删除，历史业务数据保留。
- `super_admin` 仅作为“权限管理”的内部兼容编码，不再自动获得点检、规则或项目管理能力。
- 系统已保护至少保留一个启用且具备权限管理的账号，并阻断停用、删除当前登录账号和取消自己的权限管理权限。
- 新增用户、权限变更、启用/停用、删除访问配置和系统设置保存已有基础审计日志，并已有专项断言覆盖。
- 项目档案、业务任务、检查项、整改项、待跟进项和报告接口已按 MVP 口径补范围校验：`inspection_engineer` 仅能访问本人负责的项目及其关联点检任务/派生数据（以 `projects.mq_user_id` 为准；无 MQ 人员的历史数据按任务创建人兼容），`project_admin` 可访问全量业务数据，组合权限取并集，`super_admin` 不默认获得业务范围。

对抗审查确认的问题状态：

- Critical（已修复）：业务数据读取和写入接口已按方案 4.1 实现 `inspection_engineer` 本人范围、`project_admin` 全量范围、权限取并集、权限管理无业务范围。
- Important（已修复）：用户列表始终排除 `deleted` 账号，已覆盖显式 `status=deleted` 和权限筛选组合下的删除账号不可见验证。
- Important（已修复）：`GET /api/v1/users` 作为账号管理接口仅权限管理员可访问；业务筛选下拉使用脱敏的 `GET /api/v1/business-user-options`，不暴露 UID、邮箱和账号状态。
- Important（已修复最小版）：后台页已补左侧“用户与权限 / 系统设置”导航、中间账号维护与列表、右侧权限说明布局；更完整前端交互行为测试仍待补。
- Important（已修复）：前端已本地拦截当前登录用户停用、删除、取消个人权限管理权限，并在编辑当前用户后同步本地会话快照。
- Important：测试覆盖不足。普通账号启停用登录效果、已有 token 被停用后拒绝、删除账号边界和审计日志断言已有后端专项测试；仍需补更完整前端行为测试。
- Important（已修复）：README 默认后端测试命令已覆盖全部 `backend/tests`；`uv run pytest` 当前仍暂不作为默认入口。

## 最小任务清单
- [x] T01：实现 `POST /api/v1/auth/login`；验收：有效 UID 可登录，停用 UID 被阻断。
- [x] T02：实现 `GET /api/v1/auth/me`；验收：返回当前用户与权限集合。
- [x] T03：实现用户列表、创建用户接口；后台最小列表、创建、搜索、权限/状态筛选已接通，`deleted` 账号始终不出现在账号管理列表中。
- [x] T04：实现后端用户权限更新接口；验收：同一 UID 可配置多个权限。
- [x] T05：实现启用、停用、删除访问配置；接口已有，普通账号启停用登录效果、已有 token 停用后拒绝、删除账号边界已有专项测试。
- [x] T06：实现系统设置读取与保存；验收：自动检查开关可持久化并记录保存人。
- [ ] T07：实现前端登录页和后台账号管理页；当前后台页支持最小账号维护、当前用户前端保护、会话同步和后台三栏/分区导航，仍需补更完整行为测试。
- [x] T08：写入基础权限变更审计日志；新增、启停用、权限变更、删除访问配置和系统设置保存已有审计断言。
- [x] T09：实现业务数据权限范围校验；验收：点检执行仅本人负责范围（项目 MQ 人员口径），项目管理可全量查看/管理，组合权限取并集，权限管理不默认获得任何业务范围。

## 验收标准
- [x] UID 登录流程可跑通。
- [x] 当前用户接口可返回权限集合。
- [x] 后端权限支持叠加，不互相收窄。
- [x] 后端至少保留一个启用的权限管理员。
- [x] 历史数据不因账号停用或删除而丢失。
- [ ] 前端可维护同一用户的多权限，并通过行为测试验证。
- [ ] 前端可启用、停用账号，并展示明确状态变化；当前用户不可停用。
- [ ] 前端可删除访问配置，且不能删除当前用户或最后一个权限管理员。
- [ ] 无权限用户进入后台时只读或不可操作，并有明确提示；需行为测试覆盖。
- [x] 权限维护、账号启停用、删除访问配置和审计日志有专项测试覆盖。
- [x] 业务数据访问范围符合方案 4.1，不因“已登录”或“权限管理”而获得业务数据范围。

## 注意事项
CheckFlow 不维护公司账号密码；MVP 可使用本地密码校验或模拟认证，但接口边界需保留公司账号体系接入点。
